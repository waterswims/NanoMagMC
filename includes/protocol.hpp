#ifndef _PROT
#define _PROT

#include <iostream>

void set_protocol(
    int proto_code,
    float*& var1_list,
    float*& var2_list,
    int& var1_size,
    int& var2_size,
    float* Hs,
    float* Ts,
    int H_size,
    int T_size);

#endif
