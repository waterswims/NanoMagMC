#include <iostream>

void set_protocol(
    int proto_code,
    double*& var1_list,
    double*& var2_list,
    int& var1_size,
    int& var2_size,
    double* Hs,
    double* Ts,
    int H_size,
    int T_size);
